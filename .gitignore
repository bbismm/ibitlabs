# ── Secrets & Credentials ──
.env
access_codes.json
customer_keys/
cdp_api_key.json

# ── Private Strategy Files (Alpha) ──
sol_sniper_*.py
sol_micro_grid.py
sol_hybrid_*.py
backtest_*.py
hl_backtest.py
trendrider_backtest.py
signals.py
signals_harness.py
crazy_*.py
futures_grid.py
futures_signals.py
grid_engine.py
config.py
crazy_config.py
sol_sniper_config_shadow.py
optimizer_results*.json
agents/signal_agent.py
agents/signals_price_agent.py
monitors/regime_agent.py
run_sniper.sh
start_sniper*.sh
start_crazy*.sh
start_signals.sh
scripts/backtest_*.py
scripts/regime_watch.py
web/functions/api/signal*.js
web/functions/api/sniper*.js
web/public/signals.html

# ── Local-only handoff docs & archives ──
HANDOFF_*.md
web/_archive/

# ── State & Data ──
*.db
*.db-shm
*.db-wal
*.log
*_state.json
grid_state*.json
sol_sniper_state*.json
crazy_state.json
report_state.json
monitor_state.json

# ── LaunchAgent plists (contain tokens) ──
scripts/*.plist

# ── Generated Output ──
reports/
recordings/
logs/
og_images/
growth_state/

# ── Backups ──
*.paper_backup
*.bak

# ── Web Frontend ──
web/.wrangler/
web/node_modules/

# ── System ──
__pycache__/
.DS_Store
.fuse_hidden*
*.pyc

# alert cooldown state — runtime only, not versioned
state/alert_cooldowns/
.claude/
.wrangler/
.env.bak*
MARKETING-AUDIT.md
drafts/
out/
scratch/

mcp-server/node_modules/
mcp-server/dist/
.npmrc

# ── P0 cleanup 2026-05-08: founder lab + strategy IP not for public ──
audit_export/
lab-journal/
_backtest_cache/

# ── P2 cleanup 2026-05-08: external tool clones (kept locally, not for public) ──
# Each subdir under tools/ is a clone of an external public GitHub project
# (MusicGPT, ShortGPT, klingCreator, ACE-Step, MoneyPrinterTurbo, etc).
# They were tracked as broken submodule pointers without a .gitmodules file.
# Untracked from git but preserved on disk for local use.
content/daily-series/tools/*/

# ── P3 cleanup 2026-05-13: operator-local workspaces ──
# Internal review-prep notes, candle caches, ipynb scratch, and the local
# pixel-art IDE workspace. None of these are fork-safe.
notes/
pixel-office/
notebooks/.candles_cache/
notebooks/_build_nb.py
notebooks/_smoke_test.py
notebooks/three_way_analysis.ipynb
notebooks/report.html
notebooks/lab_dashboard_notion_draft.md

# receipt-viewer absolute-path symlinks (point at gitignored audit_export/)
# Forkers populate via scripts/sync_receipt_viewer.sh against their own chain.
receipt-viewer/rule-engine-chain.jsonl
receipt-viewer/shadow-chain.jsonl
